# MongoDB connection string.
# Local install:        mongodb://127.0.0.1:27017/medqube
# MongoDB Atlas (cloud): mongodb+srv://<user>:<password>@<cluster>.mongodb.net/medqube
#password:ypSHXkTY3jBSxvdf
MONGO_URI=mongodb://127.0.0.1:27017/medqube

# Any long random string — used to sign JWTs. Change this to your own value.
JWT_SECRET=replace_this_with_a_long_random_secret_string

# How long a login session/token stays valid
JWT_EXPIRES_IN=7d

# Port the backend API runs on
PORT=5000

# URL of the frontend (for CORS) — change if your Vite dev server uses a different port
CLIENT_URL=http://localhost:3000
